# ===========================================
# 🚀 Development Environment Configuration
# ===========================================

# 🔌 Port the application will run on
PORT=3000

# ===========================================
# ☁️ Firebase Configuration
# ===========================================
# 📦 Firebase Storage Bucket
STORAGE_BUCKET=******
# 🔐 Firebase Service Account (path or JSON string)
FIREBASE_SERVICE_ACCOUNT=****** 

# ===========================================
# 📧 SMTP Email Configuration
# ===========================================
# 📡 SMTP Host and Port
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false  # Use TLS (false for Gmail TLS on port 587)

# 🔐 SMTP Credentials
SMTP_USER=example@gmail.com
SMTP_PASS=****************

# 📤 Default Sender
SMTP_FROM=example@gmail.com

# ===========================================
# 🧪 Supabase Configuration
# ===========================================
SUPABASE_SERVICE_ROLE_KEY=******
SUPABASE_BUCKET=******         
SUPABASE_URL=******             

# ===========================================
# 🔐 JWT Token Settings
# ===========================================

# --- Access Token ---
JWT_ACCESS_SECRET=your-access-secret-key        
JWT_ACCESS_EXPIRES_IN_MS=900000 # 15 minutes

# --- Refresh Token ---
JWT_REFRESH_SECRET=your-refresh-secret-key       
JWT_REFRESH_EXPIRES_IN_MS=604800000 # 7 days

# --- Security Token (Email Verification, Reset) ---
JWT_SECURITY_SECRET=your-security-secret-key 
JWT_SECURITY_EXPIRES_IN_MS=300000 # 5 minutes

# ===========================================
# 🗃️ SQLite Database (Local Development)
# ===========================================
SQLITE_DATABASE_PATH=./database/sqlite.db

# ===========================================
# 🛢️ PostgreSQL Database Configuration
# ===========================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB_Name=name
